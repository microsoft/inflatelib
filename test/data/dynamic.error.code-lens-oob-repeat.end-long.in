
# Produces a single block that should fail when reading the code length symbols for the dynamic Huffman tables since the
# final entry instructs us to repeat zero a bunch of times, exceeding HLIT + HDIST by 137 (the max we can exceed)
# NOTE: This test is valid for both Deflate and Deflate64
>1
1       # bfinal = true
10      # Compressed with dynamic codes

00000   # HLIT = 257 (0 + 257)
00000   # HDIST = 1 (0 + 1)
0000    # HCLEN = 4 (0 + 4)

# Only need long repeats
000 000 001 000

# We're expected to produce 258 codes
0 1111111 # Repeat 0 138 times
0 1101100 # Repeat 0 119 times
0 1111111 # Repeat 0 138 times, overflowing by 137
